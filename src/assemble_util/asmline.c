#include "asmline.h"

#include "common/datatypes.h"
#include "asmutil.h"

// converts a single line to binary
instr_t asmline(char *line)
{
    int len = strlen(line);
    if (/* condition */)
    {
        /* code */
    }
    
    
}
